My final project for my uni course.
It's a lib to easily create 2D graphical games, plus a randerer lib to make base OpenGL easier which the framework is based on.
both the libs are made with only c and cmake and those are the tools needed to compile it.
(also everything except the 2Dframework folder is mainly for testing, and temporarely needing libglfw3 which is added in the nix shell(I'm using a non FHS complient linux distro(NixOS) and glfw's source doesn't work with it except the nix package)).

